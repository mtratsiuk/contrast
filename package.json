{
  "name": "contrast",
  "version": "0.0.1",
  "scripts": {
    "start": "docker-compose build --force-rm --no-cache --pull && docker-compose up -d"
  },
  "author": "Mihail Tratsiuk",
  "license": "MIT",
  "devDependencies": {
    "expose-loader": "^0.7.3",
    "standard": "^8.6.0"
  },
  "standard": {
    "globals": [
      "_",
      "__DEV__",
      "__VERSION__"
    ]
  }
}
